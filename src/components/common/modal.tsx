import React from "react";

type Props = {};

const Modal = (props: Props) => {
  return (
    <div className="">
      <button className="btn">close</button>
    </div>
  );
};

export default Modal;
